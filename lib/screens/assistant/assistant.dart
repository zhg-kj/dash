import 'package:flutter/material.dart';

import 'package:dash/screens/assistant/body.dart';

class AssistantScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Body(),
      ),
    );
  }
}
